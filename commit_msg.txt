fix(vk_renderer): 修复 CI 编译错误 (主机构建)

CI 错误 (commit cd373e3 后):
1. vk_renderer.cpp:162: designator order for field
   'VkPhysicalDeviceVulkan13Features::synchronization2' does not match
   declaration order

   修复: 重新排序 designated initializers, 必须按 struct 定义顺序
   synchronization2 在前, dynamicRendering 在后 (Vulkan 1.3 规范)

2. vk_renderer.cpp:234: 'VkAndroidSurfaceCreateInfoKHR' was not declared
   原因: 主机构建 (Linux gcc 11) 没有定义 VK_USE_PLATFORM_ANDROID_KHR
   该 struct 仅在 Vulkan android platform header 中声明

   修复:
   - VkAndroidSurfaceCreateInfoKHR/vkCreateAndroidSurfaceKHR 调用
     用 #ifdef VK_USE_PLATFORM_ANDROID_KHR 守卫
   - 主机路径保持 surface = VK_NULL_HANDLE, swapchain_ready_ = false
   - 删除重复的旧代码块 (我的 edit 没替换, 只在前面添加了 ifdef)

验证:
- Windows 主机: cmake -G Ninja -- 21 个 target 全部 OK
- Android NDK 30 (本地): libGL.so 1.7MB, libEGL.so 175KB -- OK
